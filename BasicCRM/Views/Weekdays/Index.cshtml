@model IEnumerable<BasicCRM.Models.Weekday>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.WeekdayName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.WeekdayName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.WeekdayID }) |
            @Html.ActionLink("Details", "Details", new { id=item.WeekdayID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.WeekdayID })
        </td>
    </tr>
}

</table>
