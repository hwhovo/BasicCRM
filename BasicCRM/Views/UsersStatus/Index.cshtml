@model IEnumerable<BasicCRM.Models.UsersStatus>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserStatusName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserStatusName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UserStatusId }) |
            @Html.ActionLink("Details", "Details", new { id=item.UserStatusId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UserStatusId })
        </td>
    </tr>
}

</table>
